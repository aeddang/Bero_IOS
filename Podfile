# Uncomment the next line to define a global platform for your project
platform :ios, '14.1'

target 'Bero' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Bero
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  
  target 'BeroTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BeroUITests' do
    # Pods for testing
  end

end
